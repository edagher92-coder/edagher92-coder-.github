# CLAUDE.md

Guidance for Claude Code (and other AI assistants) working in this repository.

## Repository purpose

**Unclear — flagging for the owner rather than inventing one.** This repo is
literally empty (no commits, no files besides `.git`), and its name,
`edagher92-coder-.github`, is unusual alongside the fact that this account
already has a separate, populated repo named plain `.github`
(`edagher92-coder/.github`, which holds the account-wide "father rule"
`CLAUDE.md`). A second `-.github`-suffixed repo could be:

- a duplicate or accidental fork-in-progress of `edagher92-coder/.github`,
- a staging area reserved for a future purpose (e.g. a GitHub org-level
  `.github` special repo, or a template repo), or
- something else entirely that hasn't been set up yet.

Do not assume any of these and do not backfill content to match one of them.
Ask the owner what this repo is for before adding structure or automation.

> Status: the repository is in its initial state — no commits, no
> directories, nothing to reorganize yet. This file exists only to document
> that state and point at the standing account-wide rules; update it once the
> owner clarifies the repo's actual purpose.

## Standing rules that already apply here

Even though this repo is empty, the account-wide father rule at
`edagher92-coder/.github/CLAUDE.md` applies to every repo under this account,
current and future — including this one. See that file for the full
token-efficiency policy (default model, no self-re-arming check-in loops, PR
webhook-only monitoring, the `/skill-router` and `/auto-escalate` standard
kit, etc.). Do not duplicate that content here; treat it as inherited.

## What NOT to do

- Don't invent a purpose, structure, or content for this repo — it hasn't
  been assigned one yet.
- Don't copy the `edagher92-coder/.github` CLAUDE.md content into this repo
  wholesale; these are two distinct repos.
- Don't pre-create empty scaffolding directories (`agents/`, `skills/`, etc.)
  on spec — add them only once there's a real purpose and real content.

## Next step

Whoever picks this up next should confirm with the owner (Elie Dagher) what
this repo is actually for, then replace this file's "Repository purpose"
section with the real answer.
