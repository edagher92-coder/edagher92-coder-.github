# edagher92-coder-.github

This repo is a **reserved placeholder** — the account-wide Claude Code
defaults it was named for live in the canonical special repo:

➡ **[`edagher92-coder/.github`](https://github.com/edagher92-coder/.github)**

That repo carries:

- `CLAUDE.md` — the account-wide **father rule** (token-efficiency hard rules,
  Sonnet-default routing, no self-re-arming loops, lean plugins).
- `claude-defaults/` — the standard kit (`skill-router`, `auto-escalate`)
  applied to any repo with `bash claude-defaults/bootstrap.sh`.
- `PROJECTS.md` — the tidy index of every project, its repos, skills, docs,
  and session recipe.

Upstream source of truth for the defaults themselves:
`edagher92-coder/Claude-code-` → `claude-config/` (fix there first, then
re-copy).

Nothing else should be added here — new account-wide content belongs in
`edagher92-coder/.github`. This file exists so the repo states its own status
instead of sitting empty.
